#!/usr/bin/env zsh
# Disable delta because it doesn't respect text moves in diff
# if which delta > /dev/null 2>&1; then
#   delta --plus-color="#013c00" --minus-color="#480001" "$@"
# else
#   diff-highlight "$@" | less --LINE-NUMBERS
# fi
diff-highlight "$@" | less --LINE-NUMBERS
