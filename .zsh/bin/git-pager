#!/usr/bin/env zsh
if command -v delta > /dev/null 2>&1; then
  delta \
    --file-style "yellow auto bold ul" \
    --file-decoration-style "none" \
    --hunk-header-style "syntax #333333" \
    --hunk-header-decoration-style "none" \
    "$@" ||
    ( diff-highlight "$@" | less --LINE-NUMBERS )
else
  diff-highlight "$@" | less --LINE-NUMBERS
fi
